# Local AI runtime state
.atl/

# Raw immutable archive (D2) — sha256-tracked via archive-manifest.json, not git
archive/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Node
node_modules/
.next/
apps/web/node_modules/
*.tsbuildinfo

# Env files with real secrets (never commit; see
# apps/web/environment-variables.example.txt for the placeholder template)
.env
.env.local
.env.*.local

# Playwright run output
test-results/
apps/web/test-results/
apps/web/playwright-report/
