# Copy to .env.local (gitignored) and fill with real values. Never commit
# real Supabase keys, service-role keys, or fiscal credentials.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Only needed for e2e (Playwright) runs that seed a fixture auth user via the
# Supabase Admin API. Never point this at a production project.
SUPABASE_SERVICE_ROLE_KEY=

# Seeded fixture user for e2e/auth.spec.ts. Local/ephemeral only — this is
# not a real fiscal's credential.
VOTUS_E2E_TEST_USER_EMAIL=
VOTUS_E2E_TEST_USER_PASSWORD=
